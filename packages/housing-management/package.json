{
    "name": "housing-management-workspace",
    "version": "0.1.0",
    "private": true,
    "main": "apps/handler.ts",
    "scripts": {
        "build": "esbuild apps/handler.ts --bundle --minify --sourcemap --platform=node --target=es2022 --outfile=dist/index.js",
        "build:watch": "yarn workspace housing-management-workspace run build --watch"
    },
    "dependencies": {
        "envalid": "^7.3.1",
        "esbuild": "^0.17.15",
        "http-status-codes": "^2.2.0"
    }
}
